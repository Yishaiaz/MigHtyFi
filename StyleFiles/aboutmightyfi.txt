MightyFi is a system that predict (given a audio file, name, and artist's name of a song)
the how successful the song will be (predict the range of the views it will get on Youtube)
Our system is extracting features from the audio file(musical features) and from the lyrics(textual features)
and predict with a ML model the range of the views.
Enjoy the using :) 